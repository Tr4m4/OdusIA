FROM node:20-alpine
ENV NODE_ENV=production
WORKDIR /app
COPY index.html odus-hub-server.js ./
EXPOSE 8760
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
  CMD wget --no-verbose --tries=1 --spider http://127.0.0.1:8760/ || exit 1
CMD ["node", "odus-hub-server.js"]
